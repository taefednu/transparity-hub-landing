{
  "buildCommand": "git lfs pull && pnpm install && pnpm run build",
  "outputDirectory": "build",
  "framework": "vite",
  "installCommand": "pnpm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

