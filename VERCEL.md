# 🚀 Деплой на Vercel

## Пошаговая инструкция

### 1. Создание проекта

1. Зайдите на [vercel.com](https://vercel.com) и войдите через GitHub
2. Нажмите **"Add New..."** → **"Project"**
3. Выберите репозиторий `taefednu/transparity-hub-landing`

**При запросе доступа GitHub:**
- ✅ **Рекомендуется:** Выберите **"Only select repositories"** → выберите `transparity-hub-landing`
- Или дайте доступ ко всем репозиториям (удобнее для будущего)

4. Нажмите **"Create"**

### 2. Настройки проекта (ВАЖНО!)

После создания проекта откроется экран настроек. Укажите:

- **Framework Preset**: `Vite` (или "Other")
- **Root Directory**: `frontend` ⚠️ **ОБЯЗАТЕЛЬНО!**
- **Build Command**: `npm run build` (или оставьте по умолчанию)
- **Output Directory**: `build` ⚠️ **ОБЯЗАТЕЛЬНО!**
- **Install Command**: `npm install` (или оставьте по умолчанию)

### 3. Деплой

Нажмите **"Deploy"** и ждите 1-2 минуты.

### 4. Готово! ✅

Получите URL: `https://your-project.vercel.app`

---

**Важно:** Если не указать **Root Directory = `frontend`**, билд не пройдет, так как проект находится в подпапке.

